import { SubgraphToken } from 'types';
import { PrismaPool, PrismaToken } from './types';

export function mapVertekPoolToSubgraphPool(pool: PrismaPool) {}

// export function mapVertekTokenToSubgraphToken(
//     tokens: VertekToken[]
// ): SubgraphToken[] {
//     const sTokens: SubgraphToken[] = [];
//     tokens.forEach((vt) => {
//         sTokens.push({
//             address: vt.address,
//         });
//     });
// }
